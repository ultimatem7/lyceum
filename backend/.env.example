# Server Configuration
PORT=5000
NODE_ENV=development

# Database - Choose ONE option:
# Option 1: Local MongoDB
MONGODB_URI=mongodb://localhost:27017/lyceum

# Option 2: MongoDB Atlas (recommended for production)
# MONGODB_URI=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/lyceum?retryWrites=true&w=majority

# Security
JWT_SECRET=CHANGE_THIS_TO_A_LONG_RANDOM_STRING_FOR_PRODUCTION
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"